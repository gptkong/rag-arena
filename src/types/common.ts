/**
 * 通用类型定义
 *
 * 用于跨模块共享的基础类型
 */

import type { Dayjs } from 'dayjs'

// ============================================================================
// 时间范围类型
// ============================================================================

/**
 * 日期范围类型
 * 用于时间筛选场景，支持 null 表示未选择
 */
export type DateRange = [Dayjs | null, Dayjs | null] | null
