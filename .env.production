# Production 模式配置
# 打包后正式环境接口

# API 模式：mock | dev | prod
VITE_API_MODE=prod

# 是否使用 mock 数据（1=使用mock，0=使用真实接口）
VITE_USE_MOCK=0

# API 基础地址（正式环境地址，需要根据实际部署环境修改）
VITE_API_BASE_URL=https://api.example.com

# 开发服务器代理目标（生产环境不使用）
VITE_DEV_PROXY_TARGET=
