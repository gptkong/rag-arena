# Mock 模式配置
# 使用 mock 数据，不调用真实接口

# API 模式：mock | dev | prod
VITE_API_MODE=mock

# 是否使用 mock 数据（1=使用mock，0=使用真实接口）
VITE_USE_MOCK=1

# API 基础地址（mock 模式下不使用）
VITE_API_BASE_URL=

# 开发服务器代理目标（mock 模式下不使用）
VITE_DEV_PROXY_TARGET=
